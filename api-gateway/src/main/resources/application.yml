server:
  port: 8090

spring:
  main:
    web-application-type: reactive
    allow-bean-definition-overriding: true
  application:
    name: api-gateway

  cloud:
    gateway:
      # --- Configuración global de CORS ---
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:3000"   # Frontend React en desarrollo
              - "http://localhost:5173"   # Frontend Vite
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true

      # --- Rutas hacia cada microservicio ---
      routes:

        # Identity Service
        - id: identity-service
          uri: http://identity-service:8081
          predicates:
            - Path=/api/v1/identity/**

        # Patient Service
        - id: patient-service
          uri: http://patient-service:8083
          predicates:
            - Path=/api/v1/patients/**

        # Medical Staff Service
        - id: medical-staff-service
          uri: http://medical-staff-service:8082
          predicates:
            - Path=/api/v1/medical/**

        # Configuration Service (part of Medical Staff Service)
        - id: configuration-service
          uri: http://configuration-service:8085
          predicates:
            - Path=/api/v1/configuration/**

        # Appointment Service
        - id: appointment-service
          uri: http://appointment-service:8084
          predicates:
            - Path=/api/v1/appointments/**

# --- Swagger ---
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method

# --- URLs internas de los servicios (usadas por el Saga) ---
services:
  identity:
    url: http://identity-service:8081
  patient:
    url: http://patient-service:8083
  medical-staff:
    url: http://medical-staff-service:8082
  appointment:
    url: http://appointment-service:8084

